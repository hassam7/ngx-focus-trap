import { NgxFocusTrapDirective } from './ngx-focus-trap.directive';

describe('NgxFocusTrapDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFocusTrapDirective();
    expect(directive).toBeTruthy();
  });
});
